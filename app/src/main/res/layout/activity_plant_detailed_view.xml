<?xml version="1.0" encoding="utf-8"?>
<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:padding="16dp">

    <!-- Image View for Plant Image -->
    <ImageView
        android:id="@+id/imageViewPlantDetail"
        android:layout_width="match_parent"
        android:layout_height="200dp"
        android:scaleType="centerCrop"
        android:src="@drawable/ic_launcher_background" /> <!-- Replace with your default image or drawable -->

    <!-- Edit Text for Plant Title -->
    <EditText
        android:id="@+id/editTextPlantTitle"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:hint="Plant Title"
        android:inputType="text"
        android:layout_marginTop="16dp"/>

    <!-- Edit Text for Plant Information -->
    <EditText
        android:id="@+id/editTextPlantInfo"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:hint="Plant Info"
        android:inputType="textMultiLine"
        android:gravity="top"
        android:minHeight="100dp"
        android:layout_marginTop="8dp"/>

    <!-- Horizontal Layout for Buttons -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:layout_marginTop="16dp"
        android:gravity="end">

        <!-- Confirm Button -->
        <Button
            android:id="@+id/buttonConfirm"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Confirm" />

        <!-- Cancel Button -->
        <Button
            android:id="@+id/buttonCancel"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Cancel"
            android:layout_marginStart="16dp"/>

    </LinearLayout>
</LinearLayout>
